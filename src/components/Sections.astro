---
// Empty 100vh snap-scroll sections. Content will be added later.
const sections = ['section-1', 'section-2', 'section-3'];
---

{
  sections.map((cls) => (
    <section class={`snap-section ${cls}`} />
  ))
}